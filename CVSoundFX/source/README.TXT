Run using the following command:

java -jar CVSoundFX.jar

or use the Windows EXE CVSoundFX.exe

Note: if this runs with an Error:  Can't find java.home ?? add the following parameters
CVSoundFX.exe ./TG -Djava.home="<path to you JRE here>"